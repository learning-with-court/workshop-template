// TEMPLATE: replace with real lesson tests.
import { describe, it, expect } from "vitest";

describe("template lesson", () => {
  it("placeholder — replace when authoring the real lesson", () => {
    expect(true).toBe(true);
  });
});
