# TEMPLATE: fill in every field marked TODO before pushing the registry entry.
# This file is consumed by:
#   - platform manifest fetcher (drives the catalog + Lambda)
#   - scripts/lint-manifest.ts (validates schema + cross-checks fs)
#
# Schema source of truth:
#   platform/packages/server/src/manifest/schema.ts

id: template-workshop
# compose model: the tag namespace + .workshop/<short>/ segment the generator emits.
# build-workshop substitutes WORKSHOP_SHORT with the real short id (e.g. "my-ws").
composeShort: WORKSHOP_SHORT
title: "TODO: Workshop title"
status: available  # ships available from day one; see docs/WORKSHOP_STANDARD.md
repo: learning-with-court/template-workshop
tagline: "TODO: One-line hook describing what the learner walks away with."
summary: |
  TODO: 2–4 sentence paragraph. What does the learner build? What's the
  end state? Who is this for? Read the mcp-workshop summary for tone.
difficulty: beginner  # beginner | intermediate | advanced
tags:
  - todo-tag
install: "set up the template workshop"  # what the user types to install via the setup skill
youWillBuild:
  - "TODO: bullet 1 — concrete artifact the learner ships."
  - "TODO: bullet 2."
  - "TODO: bullet 3."
prerequisites:
  - term: "TODO: prereq term"
    desc: "TODO: one-sentence description of what's expected."
# Lessons are referenced by slug (kebab-case). The slug is the lesson's
# single identity: phase ref + lesson.yaml `id` + dir `lesson_<slug>` +
# walker `lesson-<slug>.md` all share it. No numbers. See docs/WORKSHOP_STANDARD.md.
phases:
  - id: A
    title: "TODO: phase title"
    lessons:
      - example

# Optional: series membership. Omit entirely for standalone workshops.
# Add this block when the workshop is part of a multi-workshop series
# that learners should walk in order (e.g. the Claude Certified Architect series).
# The same values must be mirrored in the `series` field of the workshops.json
# registry entry so the landing site can group and link series members.
#
# series:
#   id: my-series-id          # kebab-case; shared by all workshops in the series
#   title: "My Series Title"  # human-readable; same for all workshops in the series
#   order: 1                  # 1-indexed position within the series
